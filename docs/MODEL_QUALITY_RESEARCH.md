# Model Quality Research

This report documents model-quality diagnostics and limited-budget experiments on the real local MIT-BIH records. It is technical research analysis only, not clinical validation.

## Baseline

Current preserved checkpoint:

```text
checkpoint: artifacts/models/best_model.pt
model_type: baseline_cnn
accuracy: 0.6284
macro_f1: 0.2824
weighted_f1: 0.6057
roc_auc_ovr_macro: 0.6663
```

Per-class F1:

```text
N: 0.8975
V: 0.4527
A: 0.0014
L: 0.0000
R: 0.0606
```

## Diagnostics

Generated by:

```bash
python -m scripts.diagnose_model_quality --config configs/default.yaml --checkpoint artifacts/models/best_model.pt
```

Findings:

- Training/evaluation windows are annotation-centered MIT-BIH beat windows with fixed length `360`.
- API inference uses detected R-peaks, so API behavior depends on peak-detection quality.
- Test `A` support is low (`139` beats), making A metrics high-variance.
- `L` has non-trivial train and test support, but baseline F1 remains `0.0000`, which points to record-level morphology generalization failure.
- `R` is mostly confused with other morphology classes and remains severe despite threshold tuning.
- Training loss decreases while validation macro F1 plateaus, indicating overfitting and record-level domain shift.

## Limited-Budget Model Comparison

Generated by:

```bash
python -m scripts.compare_models --config configs/default.yaml
```

The comparison uses a deliberately small CPU-safe budget:

```text
compare_epochs: 2
compare_max_train_samples_per_class: 300
isolated subprocess per architecture: true
```

Results:

| Model | Accuracy | Macro F1 | Weighted F1 | A F1 | L F1 | R F1 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: |
| baseline_cnn | 0.4451 | 0.1953 | 0.4807 | 0.0000 | 0.0000 | 0.0000 |
| resnet1d | 0.7325 | 0.3119 | 0.6334 | 0.0000 | 0.6823 | 0.0000 |
| inceptiontime | 0.0734 | 0.0504 | 0.0371 | 0.0000 | 0.1018 | 0.1503 |
| cnn_lstm | 0.1597 | 0.0761 | 0.0637 | 0.0000 | 0.0028 | 0.3709 |

The best limited-budget experiment was `resnet1d`; it was useful diagnostically because it recovered `L`, but it was not suitable for promotion.

```text
artifacts/models/best_model_research.pt
```

That earlier research checkpoint has been superseded locally by the high-budget `cnn_lstm` candidate below. `artifacts/models/best_model.pt` remains the default baseline artifact.

## Higher-Budget Research Pass

Generated by:

```bash
python -m scripts.run_high_budget_experiments --config configs/default.yaml --epochs 5 --cap 2500
```

This run used the same record-level train/validation/test split, real MIT-BIH WFDB data, focal loss, class-balanced sampling, lower-strength augmentation, and per-class threshold tuning. It is still a local CPU-budgeted research run, not full uncapped training.

Promotion required:

- macro F1 greater than `0.2824`
- non-zero `A`, `L`, `R`, and `V` F1
- anti-collapse check passing
- at least four predicted classes

Results are saved under `reports/experiments/high_budget/`:

| Experiment | Status | Accuracy | Macro F1 | Weighted F1 | N F1 | V F1 | A F1 | L F1 | R F1 | Promotion |
| :--- | :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :--- |
| `resnet1d_maxabs_focal_balanced` | completed | 0.7693 | 0.4567 | 0.7053 | 0.8699 | 0.7936 | 0.0000 | 0.0000 | 0.6200 | no |
| `resnet1d_zscore_focal_balanced` | completed | 0.7003 | 0.3211 | 0.5866 | 0.8361 | 0.7106 | 0.0000 | 0.0000 | 0.0588 | no |
| `inceptiontime_maxabs_focal_balanced` | completed | 0.6745 | 0.2949 | 0.6347 | 0.9512 | 0.5221 | 0.0010 | 0.0000 | 0.0000 | no |
| `cnn_lstm_maxabs_focal_balanced` | completed | 0.5520 | 0.3295 | 0.5890 | 0.8390 | 0.7493 | 0.0153 | 0.0026 | 0.0414 | yes |

The best candidate is:

```text
artifacts/models/best_model_research.pt
model_type: cnn_lstm
normalization: maxabs
macro_f1: 0.3295
```

This candidate passes the narrow research promotion rule because it improves macro F1 and has non-zero `A`, `L`, `R`, and `V` F1. It is still not clinically reliable. `A`, `L`, and `R` remain weak, especially `L` at `0.0026` F1 and `R` at `0.0414` F1.

## Grouped Fold Check

Generated by:

```bash
python -m scripts.cross_validate --config configs/default.yaml
```

This evaluates the current checkpoint across grouped held-out record folds. It does not retrain per fold.

```text
macro_f1_mean: 0.5240
macro_f1_std: 0.1438
accuracy_mean: 0.8004
```

The high fold variance reinforces that MIT-BIH record identity and morphology distribution are major sources of instability.

## Interpretation

A/L/R are failing for different reasons:

- `A`: low held-out support and high confusion with `V`.
- `L`: baseline model does not learn record-generalizable bundle-branch morphology; `resnet1d` can recover it under a limited run.
- `R`: strong record-level failure and unstable architecture behavior.

## Next Research Step

Run uncapped repeated grouped-record training for `resnet1d` and `cnn_lstm`, then add fold-aware threshold calibration. The next promotion rule should be stricter than this pass: require macro F1 improvement plus minimum per-class recall/F1 floors for `A`, `L`, and `R`, not merely non-zero scores.
